server:
  port: 8080
spring:
  profiles:
    active: dev
  application:
    name: visit-service
  output:
    ansi:
      enabled: never
xo:
  cache:
    config-file: classpath:redisson-${spring.profiles.active}.yml
logging:
  level:
    root: WARN