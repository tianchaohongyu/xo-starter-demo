server:
  port: 8000
spring:
  profiles:
    active: dev
  application:
    name: admin-service
  output:
    ansi:
      enabled: never
xo:
  cache:
     x-auth-token: false
logging:
  level:
    root: WARN